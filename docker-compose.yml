services:
  whatsapp-calendar-bot:
    build: .
    container_name: wa-calendar-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot_data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot_data:
